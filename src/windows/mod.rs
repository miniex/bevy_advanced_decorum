use crate::settings::DecorumSettings;
use bevy::prelude::*;
use bevy::window::RawHandleWrapper;

pub fn setup_window_decorations(
    _entity: Entity,
    _window: &Window,
    _raw_handle_wrapper: &RawHandleWrapper,
    _settings: &DecorumSettings,
) {
}

pub fn update_window_decorations(
    _entity: Entity,
    _window: &Window,
    _raw_handle_wrapper: &RawHandleWrapper,
) {
}
